<%= form_with(model: faculty, local: true, :class => "form-basic") do |form| %>
  <% if faculty.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(faculty.errors.count, "error") %> prohibited this faculty from being saved:</h2>

      <ul>
      <% faculty.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-row">
    <%= form.label :name %>
    <%= form.text_field :name, id: :faculty_name %>
  </div>

    <div class="form-row">
      <%= form.label :users_id %>
      <%= current_user.id %>
    </div>

  <button class="form-row actions">
    <%= form.submit %>
  </button>
<% end %>
