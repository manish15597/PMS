<%= form_with(model: student, local: true, :class => "form-basic") do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
      <% student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-row">
    <%= form.label :name %>
    <%= form.text_field :name, id: :student_name %>
  </div>

  <div class="form-row">
    <%= form.label :reg_no %>
    <%= form.number_field :reg_no, id: :student_reg_no %>
  </div>

  <div class="form-row">
    <%= form.label :users_id %>
    <%= current_user.id %>
  </div>

  <div class="form-row actions">
    <%= form.submit %>
  </div>
<% end %>
