<p id="notice"><%= notice %></p>

<h1>Student Courses</h1>

<table>
  <thead>
  <tr>
    <th>Course</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @student_course.each do |student_course| %>
      <tr>
        <td><%= Course.find(student_course.course_id).course_name %></td>
        <td><%= link_to 'Show', student_course %></td>
        <td><%= link_to 'Edit', edit_student_course_path(student_course) %></td>
        <td><%= link_to 'Destroy', student_course, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>

<!--ang_brac_here%= link_to 'New Student Course', new_student_course_path %>-->
<%= link_to "Choose Course", choose_course_path, :class => 'navbar-link' %>
